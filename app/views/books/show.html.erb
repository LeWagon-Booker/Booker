<div class="picture-and-info d-flex justify-content-between">
  <img src="<%= @book.image_url %>" alt=""> <!-- need to replace this with image tag -->
  <div class="info flex-grow-1 px-4">
    <h1><%= @book.title %></h1>
    <h2><%= @book.author %></h2>
    <h2><%= @book.year %></h2>
    <h2><%= @book.month %></h2>
    <h2><%= @book.user_id %></h2>
  </div>
</div>
